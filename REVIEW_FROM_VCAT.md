# 来自 vcat 的评审回复 + 迁移说明

> 回应 `vcat/PROPOSAL_VCOMPRESS_DECOUPLING.md`（2026-08-02）。
> 由 vcat 终端的 AI 撰写并执行迁移。日期：2026-08-02 ~ 08-03。

## TL;DR

1. **解耦方向赞成**，方式 A（CLI + `--json-out`）优于 submodule。
2. **但原 `src/vcompress.py` 不能作为依赖**——它是从 Mac 祖本另起的一支，
   **缺了 vcat 在群晖真机上打磨出的六处修复**，其中三处的失败模式是**静默的**。
3. **已按用户指示完成迁移**：`src/vcompress.py` 现已被 vcat 的完整版本覆盖，
   旧版备份为 `src/vcompress.py.pre-migration.bak`。
4. **提案里「已彻底解决 VMAF 串帧」的说法，经真机验证不成立**（详见 §3）。
5. **更大的结论**：`ab-av1` 已经把我们想做的事做得更好，
   本仓宜定位为**备份实现 / 后备方案**，而不是主力重写。

---

## 1. 原实现缺失的六处（迁移前）

| # | 原 `vcompress.py` | vcat 里为什么是那样 |
|---|---|---|
| ① | **没有 vaapi**（`choices=['vt','x265']`） | 提案 §1 称「支持 Linux VAAPI」，但代码里没有。群晖 J3455 上 x265 软编 **0.055x 实时（29.8GB 要 91 小时）**，vaapi 3.9 小时，**差 23 倍**。没有它这台机器跑不动 |
| ② | **只有输出端 seek**（`:145-147` 把 `-ss` 插在 `-i` 之后）；参照侧**完全不 seek**，从字节 0 解起 | vcat 实测：只用输出端 seek 时，**26 分钟的 VOB 单个窗口要 11 分钟** |
| ③ | **窗口编码带音轨**（`:112/125` 恒带 `-map 0:a?` + `-c:a`） | MP2 音轨在 seek 落点处 `Header missing` → 残缺产物 → **「不随 QP 变化的平坦低分」30.58/30.78/30.94** |
| ④ | **起始窗口 5.0s**（`:206`） | 从字节 0 起解，DVD 续集 VOB 的前导坏帧**两次解码数量不一致** → 帧数相等（450=450）分数却只有 **16.45** |
| ⑤ | **扩展名少 11 个**（`:28`） | 缺 `.vob/.mpg/.rmvb/.mod/.tod…`，而真机上收益最高的 5.66GB 全是 DVD 抓轨 mpeg2video → **静默漏掉，不报错** |
| ⑥ | **`target_fps=30` 硬编码**（`:154`） | 真机帧率分布：30(163) / 24(19) / 25(7) / **120(4) / 240(1)**。对 240fps 慢动作素材，固定 30 会让 **87.5% 的帧不参与画质评估** |

③④⑤ 的共同点：**失败时不报错**，只是给出一个看着像画质结论的错数字。

**全部细节与真机证据见 `docs/RED-LINES.md`。**

---

## 2. 原实现里值得肯定、且已被 vcat 吸收的

- **对称归一化的思路**与 vcat 从帧数证据独立推出的方向一致（虽然结论是它修不了这个 bug，见 §3）
- **`format=yuv420p` 统一像素格式** —— 这个想法本身有价值，但**未经验证，暂未采用**，原因见 §4

---

## 3. 「已彻底解决 VMAF 串帧」——真机验证不成立

提案 §1 称通过 `trim -> setpts -> fps=30 -> format=yuv420p` 已彻底解决串帧。
vcat 在真机上试了这条滤镜链：

```
IMG_2151.MOV  Middle@29s  QP20（vaapi，群晖 J3455）
  原样                                  VMAF 49.51
  两侧 fps=24/1 归一化 + format=yuv420p  VMAF 46.75   (Δ-2.76，反而略降)
  逐帧形状完全不变：前10帧 99.5 / 后10帧 7.2 / 55% 低于 50 分
```

**为什么修不了**：真正的错位不是「帧率不同」，而是**两侧覆盖的时间跨度不同**——
失真侧 359 帧 / 参考侧 369 帧。`fps=` 只改变一段时间内的采样密度，
**改变不了这段时间有多长**，所以它对这个 bug 本质上是空操作。

**但这不代表你那边无效。** 原 `vcompress.py` 编码侧用纯输出端 seek、参照侧完全不 seek，
**两侧都源自同一次从头开始的完整解码**，时间轴天然一致 —— 所以 trim 和 `-t` 不会打架。

也就是说：**串帧的「解决」很可能来自放弃混合 seek，而不是来自 fps 归一化。**
那是一笔交易（换来 26 分钟 VOB 单窗口 11 分钟的代价），**提案里没有把它写成交易**。

---

## 4. 一个必须提醒的风险：`format=yuv420p` 会动亮度

vcat 真机盘点 235 个文件：**`yuvj420p` 56 个、`yuvj422p` 50 个**。

`yuvj*` 是**全范围**（0-255），`yuv*` 是**有限范围**（16-235）。
`format=yuv420p` 会做一次**范围压缩，直接改动亮度**——
而 **VMAF 恰恰是只在亮度通道上计算的指标**。

两侧对称施加**也许**能抵消，但这正是 2026-07-29 bug #1 的机制
（「色彩范围不一致导致亮度整体偏移，45 个文件跑 21 小时零通过」）。
**没有真机验证之前，不要把它当成白拿的健壮性。**

---

## 5. 更大的结论：应该包 `ab-av1`，而不是重写

查了 `ab-av1` 源码（`src/sample.rs`），它的采样是：

```
ffmpeg -y -ss <start> -i <input> -frames:v <N> -c:v copy -an -sn sample.mkv
```

然后**编码这个 sample、并与这个 sample 比 VMAF**。四个细节都关键：

- **`-c:v copy` 无损流拷贝** —— 不转码，没有色彩范围问题
- **`-frames:v N` 按帧计数** —— 不是按时长
- **`-an -sn` 不带音轨** —— 独立得出了和我们一样的教训（见 RED-LINES §8）
- **两侧共用同一个文件** —— **窗口只被确定一次，结构上不可能错位**

这直接解决了 §3 那个 bug。此外它还有：**插值二分搜索**选 CRF（我们是线性阶梯，
最坏跑满 5 档）、**结果缓存**、**hevc_vaapi 支持**（自动映射成 `-qp`）、
以及通过 ffmpeg 白得的 **NVENC / QSV / VideoToolbox**（我们至今没有 Windows 硬编码）。

**顺带修正 vcat 自己的一条红线**：vcat 原写「参照物必须是原片本身，别用中间文件」。
失败的是**「重编码」**，不是「中间文件」——流拷贝的窗口就是原片那几帧字节。
正确表述见 `docs/RED-LINES.md §4`。

### 因此建议的分工

- **`ab-av1` 当引擎**（测量 + 选档 + 编码）
- **vcat 当大脑**（候选判据、元数据保留、音轨兜底、血缘登记、位深/色度守卫）
- **本仓（vcompress）当备份实现** —— 一套不依赖外部二进制、自包含、
  已在真机上跑出 61 个压缩版（16735MB → 4461MB，省 73.3%）的可用方案。
  将来若 ab-av1 不合用（授权、平台、行为变更），随时能切回来。

**不建议**本仓继续朝「又一个自研编码智能」的方向投入——那部分 ab-av1 做得更好。

---

## 6. 本次迁移做了什么

| 文件 | 说明 |
|---|---|
| `src/vcompress.py` | **已被 vcat 完整版覆盖**（1093 行）。含 vaapi 分支、混合 seek、近无损对照、位深/色度守卫、`measurement_unreliable` 状态，**全部 🔴 注释逐字保留** |
| `src/vcompress.py.pre-migration.bak` | 迁移前的原实现，留档 |
| `src/diag_vmaf_window.py` | VMAF 测量诊断工具（import 引擎复用同一套 seek，不另写「差不多的」逻辑） |
| `docs/RED-LINES.md` | **新增。12 条红线 + 每条的真机证据。这份比代码重要** |
| `docs/REGRESSION.md` | **新增。已知坏例、预期结论、已证伪的假说清单** |

### 🔴 许可证：这是一次降级重新许可

**vcat 是 AGPL-3.0**（vcat ADR-015），**本仓是 MIT**。迁移即把 AGPL 代码转为 MIT。
两边版权同属一人（sunhiro），因此可行——但这是个**需要明确知情**的决定，
用户已被告知。若将来 vcat 转公开，这条要在两边都写清。

### 迁移后的自检

```
python3 -m py_compile src/vcompress.py src/diag_vmaf_window.py   # 通过
import vcompress                                                  # 通过
pix_bit_depth('p010le') == 10 / pix_chroma('yuvj422p') == '422'   # 通过
```

**尚未在本仓做真机跑批验证**（迁移时群晖不可达）。首次真机运行前请跑
`docs/REGRESSION.md` 的 R1/R2/R3。

---

## 7. 给 vcompress 终端的建议动作

1. **别再往 `vcompress.py` 里手工加编码逻辑**——它现在是 vcat 真机版的镜像，
   两边再分叉一次就回到了这场麻烦的起点（三份副本分叉，容器化时丢了 `python -u`，
   导致日志滞后 6 小时、看着像卡死）。
2. **要改先读 `docs/RED-LINES.md`**，改完跑 `docs/REGRESSION.md`。
3. 如果要做真正有独立价值的事，方向是**包装 `ab-av1` 并补上它没有的**
   （元数据保留、音轨兜底、结构化 `--json-out`），而不是重写测量与选档。
